# EIS合格応援アプリ — 開発メモ

Ekamai International School（バンコク）Grade 8 入試のための学習アプリ。
**試験日は 2027年1月7日**（`js/app.js` の `EXAM_DATE` に定数として固定。画面からは変更できない）。

機能の全体像は **README.md に詳しく書いてある**。ここには、コードを書くときに知っておくべきことだけを置く。

---

## 動かし方

**ビルド工程は無い。** `index.html` をブラウザで開けばそれで動く。
`file://` でも動くよう、モジュールを使わず `<script>` の読み込み順で依存を解決している。

```
index.html
  └ js/data.js → math-data.js → reading-data.js → grammar-data.js
    → math-figure.js → math-choices.js → storage.js → ai.js → app.js
```

この順番は変えないこと。`app.js` は先に読まれた 4 つのデータと
`MathFigure` / `MathChoices` / `Storage` / `AI` に依存している。

### 確認のしかた

**画面を実際に動かして確かめる。** Playwright でヘッドレス Chromium を起動し、
`file:///.../index.html` を開いてクリックしていく。壊れやすいのは次の3点なので、変更したら必ず見る。

- `pageerror` を拾って JS エラーが 0 件であること
- 変更した画面のスクリーンショットを撮って、レイアウトが崩れていないこと
- localStorage をまたぐ挙動（学習記録・APIキー）

---

## コードの書き方

このリポジトリの既存コードに合わせること。

- **コメントは日本語**。「何をしているか」ではなく **「なぜそうしたか」** を書く
- フレームワークもビルドツールも使わない。素の HTML / CSS / JS
- CSS 変数（`--primary` など）を使う。色を直書きしない
- `$('#id')` は `document.querySelector` の別名
- 要素の出し分けは `hidden` 属性。ただし **CSS で `display` を指定した要素は `hidden` が効かない**ので、
  その場合は `.foo[hidden] { display: none; }` を併記する（過去に踏んだ）

---

## データの構造

| ファイル | 中身 | 件数 |
| --- | --- | --- |
| `js/data.js` | 単語 | 3,587 |
| `js/math-data.js` | 算数 | 884 |
| `js/reading-data.js` | 長文読解 | 134 本 / 523 問 |
| `js/grammar-data.js` | 文法 | 706 問 |

各ファイルの冒頭コメントに項目の説明がある。**追加するときは必ずそれを読むこと。**

### レベルの対応

**単語・長文・文法は4段、算数は5段**（Grade 3〜7）。段数は `js/app.js` の `MAX_LEVEL` に持たせてある。
**文法だけレベル1の問題が無い**（教材が A2 から始まるため）。適応型テストは空の在庫を
飛ばす作りなので止まらないが、レベル1を前提にした処理を足すときは気をつけること。
段数を直接書かず、必ずここから引くこと（模擬試験の階段の上限が算数だけ固定値で、
最上段まで上がらないバグを2回踏んだ）。

| | 単語・長文 | 文法 | 算数 |
| --- | --- | --- | --- |
| 1 | 〜A1 | （無し） | Grade 3（小3） |
| 2 | **A2** | **A2** | Grade 4（小4） |
| 3 | **B1** | **B1** | Grade 5（小5） |
| 4 | **B2** | **B2** | Grade 6（小6） |
| 5 | — | — | Grade 7（中1） |

算数を日本の学年でなく **インター校の Grade** で区切るのは、受けるのが
EIS の Grade 8 入試だから。日本の中1にない単元（確率など）も Grade 7 に入れている。

**Grade 3 はつまずいた子が戻ってくる場所。**あまりのあるわり算・分数のはじまり・
ぼうグラフの読み取りまで入れてある。ここを飛ばすと下が抜けたまま積み上がる。

長文はレベルが上がるほど**本文を長くしている**（195語 → 265語 → 305語）。
語彙が難しくなるだけでなく読む量が増えるのが実際の試験なので、長さも難易度の一部として扱う。
長さの目安は **TOEFL Junior** の読解セクション（250〜400語）に合わせた。

### localStorage のキーと形式は壊さない

- 保存キーは `eis-app:v1`（`js/storage.js`）。**今後のアップデートでも絶対に変えない。**
- state に項目を足すのは OK（`load()` が DEFAULT_STATE で埋める）。**項目の削除・改名・意味の変更は不可。**
  形式を変えたくなったらキーはそのままで `load()` 内でマイグレーションする。
- 1日1回の自動バックアップ（`eis-app:v1:backup`）と破損時の退避（`eis-app:v1:broken`）がある。
  この仕組みを消さないこと。

### 単語のレベルは「教材での初出」で決める

**その語が最初に出てくる本文のレベル**をその語のレベルにする。教材が難易度順に
分かれているので、**外部の物差し（CEFR-J）は持ち込まない。**
付け直しは `node tools/relevel-vocab.mjs --emit`（level だけを書き換える。id は触らない）。

過去に3つの理由で実態とずれていた。同じことを繰り返さないこと。

- **語彙練習リスト由来の620語がレベル3の決め打ちだった。**リストにレベルの情報が無いため。
  教材に出てくる170語だけは初出から決められるが、残り450語は判定できない
- **読解教材しか見ていなかった。**あとから文法教材3冊が入り、同じ語がより易しい冊子に
  出ていることが分かった（piano / exam / color が B1 のままだった）
- **冊子の巻頭・巻末まで数えていた。**巻頭は「TOEFL Junior とはどんな試験か」を大人向けに
  説明した文章で、その冊子のレベルで書かれた英文ではない。ここを含めていたために
  communication / relationship / research が A2 になっていた

**何を「その冊子のレベルの英文」とみなすかは、教材を1冊ずつ確かめて決めてある。**
範囲と除外の理由は `tools/relevel-vocab.mjs` の冒頭に書いた。要点だけ:

| 冊子 | 前付 | 本編 | Directions | 後付 |
| --- | --- | --- | --- | --- |
| A2 Basic | p1–12 | **p13–136** | p14 / p125 | p137–147 |
| B1 Intermediate | p1–14 | **p15–138** | p16 / p126 | p139–143 |
| B2 Advanced | p1–14 | **p15–138** | p16 / p126 | p139–143 |

- **Directions（試験の受け方の説明）を入れない。3冊とも同じ文**が載っている（違うのは
  OCR の揺れだけ）。全レベルに同じ文がある以上レベルの手がかりにならず、含めると
  共通のサンプル問題から concept / theory / relatively / Newton が A2 になる
- **版面の柱と設問の定型文を入れない**（`PART 2`、`Questions 1-4 refer to the following
  announcement` など）。含めると announcement / biography / notice / passage が A2 になる
- 読解教材は**本文・題・語注・選択肢を入れ、設問の文は入れない**。
  `In the passage, the word X is closest in meaning to` はどのレベルにも出る定型で、
  ここだけに出る語は infer / paragraph / summarize など12語

### ID は変えない

**学習記録は ID をキーに localStorage へ保存している。** 既存の ID を振り直すと、
利用者のこれまでの記録が全部リセットされる。単語の ID 1〜530 は初期の手作りデータで、
特に慎重に扱うこと。

---

## 模擬試験の「レベル判定」について

やさしい問題から始めて難易度を上下させ、実力を推し量る適応型テスト（`js/app.js`）。

**2問続けて正解したら1段上げ、1問間違えたら1段下げる**（階段法）。この上下を繰り返すと
難易度は**正答率がおよそ7割になる高さ**で落ち着くので、**折り返し地点の平均**を推定レベルとする。

7割という線は、`js/ai.js` の機械判定が使う「正答率70%以上」と**意図的に揃えてある**。
どちらの経路で判定しても同じ基準で級を語れるようにするため。**片方だけ動かさないこと。**

---

## 算数の問題の作り方

884問のうち **m1〜m149 は手作り**、**m150 以降は `tools/` が作っている**。
手で 700 問は書けないので、単元ごとのテンプレートに数値を振って作る。

```
tools/math-gen.mjs          乱数と問題の器。rng() は固定のたねから作る
tools/math-g3〜g7.mjs       学年ごとの単元。単元1つ = 関数1つ
tools/math-relevel.mjs      既存149問の学年・分野の振り直し表と、手直し表
tools/emit-math.mjs         上をまとめて js/math-data.js を書き出す
tools/audit-math.mjs        点検
```

作り直しは `node tools/emit-math.mjs`、点検は `node tools/audit-math.mjs`。
**この順で必ず両方走らせること。**

### 守ること

- **乱数は `rng(seed)` から。`Math.random()` は使わない。**
  同じたねなら毎回同じ問題ができるので、作り直しても ID がずれない。
  ここを崩すと m150 以降の学習記録が全部ずれる
- **`emit-math.mjs` は自分の書き出したファイルを読み直す。**
  手作りぶんは `RELEVEL` に載っている ID だけを拾い、生成ぶんは毎回ゼロから作る。
  何度走らせても同じ 884 問になる（なっていなければバグ）
- **割り切れない答えを作らない。** 小数が延々と続くと「何桁書けば正解か」で揉める。
  商を先に決めてから割られる数を作る、底辺を偶数にする、といった小細工を各所でしている
- **解説は日本語で、式だけ並べない。** 「なぜそうするか」を頭に1文置く。
  点検が「解説に日本語がない」で落とす
- 分数の答えは `'3/4'` の形。採点は数として比べるので `0.75` でも `6/8` でも正解になる
- **4択の誤答は「やりがちな間違い」にする。** でたらめな数だと考えるまでもなく消せてしまう。
  生成器が `wrong: [...]` を持てば `js/math-choices.js` がそれを優先する。
  無ければ答えの形から作る（符号ミス・桁ミス・÷2 忘れ）。
  **計算の順序や正負の数は、誤答そのものが学習目標**なので必ず `wrong` を書く
- **英語でつまずかせない。** 解くのは日本語話者の子。英語圏の教室では通じても
  別の意味に読まれる言い回しは使わない（`lots of` は「たくさんの」と読まれた）。
  点検の `BAD_WORDING` に貯めてある
- **つづりは米式で統一**（meters / liters / color / candy / theater）。
  **受けるのがアメリカ式の学校（EIS）だから。**英式（metres / litres / colour）は使わない。
  算数は `audit-math.mjs` の `BR_SPELLING`、単語・長文は `audit-vocab.mjs` が弾く。
  ただし固有名詞は別（`Defence of Fort McHenry` は史実の詩の題名なのでそのまま）

### 図

図形の問題には図を付ける。**入試は図を読ませるから。**文章だけだと公式を覚えているかしか
測れず、図を読む力が測れない。

データが持つのは **形の指定だけ**で、SVG を組み立てるのは `js/math-figure.js`。

```js
{ id: 'm104', question: '…', figure: { kind: 'rect', w: 8, h: 5, unit: 'cm' } }
```

SVG をデータに直接持たせない理由は3つ。データが数倍に膨らむ・色を明暗テーマに
追従させられない・描き方を直すたびにデータを作り直すことになる。

守ること:

- **色は必ず CSS 変数を通す。** SVG に色を直書きするとダークで線が背景に沈む。
  `js/math-figure.js` は class 名だけを付け、色は `css/style.css` の `.math-figure .f-*` で決める
- **答えを図に書かない。** 「面積 81 cm² の正方形の1辺は？」の図に 9 cm と書いたら答えが割れる。
  問われている辺・角は `?`（オレンジ）にする
- **関係ない数字を書かない。** 「面はいくつあるか」に寸法は要らない（`plain: true`）
- **角の弧は `arc()` を使い、sweep-flag は 0。** SVG は y が下向きなので、
  1 にすると弧が外角側に回る（一度これで全部の角が逆に描かれた）
- 描ける形は `MathFigure.kinds`。**点検が知らない形を弾く**ので、
  新しい形は `RENDERERS` に足してから使うこと
- **ぼうグラフは値を図だけに持たせる。**問題文に数を書いたらグラフを見る必要がなくなる。
  「bar chart」と書いてあるのに図が無い問題は点検が落とす

図を足したら **必ず目で見る**。`node tools/emit-math.mjs` のあと、
Playwright で形ごとに1問ずつ並べて撮り、明暗の両方で確かめる。

### 点検が何をしているか

`audit-math.mjs` は **ジェネレータの出した答えを信じない**。
問題文を正規表現で読み直して答えを計算しなおし、データと突き合わせる（いま 70%）。
残りは文章題など読み直せない形で、そこは目視。
ほかに重複・空欄・レベルの不正、**図形の分野なのに図が無いもの**も見る
（理由があって付けないものは `NO_FIGURE_OK` に書き出す）。

マージ前にジェネレータだけ見るときは `node tools/audit-math.mjs --gen`。

## 算数の4択

本番（MAP Growth）が4択なので、**模擬試験は必ず4択**にする（設定に関係なく）。
算数タブの練習は設定で選べる（既定は4択。自由入力のほうが難しい）。

- 選択肢の並びは **ID から決める**。毎回変わると「前は B だった」で覚えられてしまう
- 誤答が正解と**数として同じ**にならないこと（`3/4` と `0.75`）。点検が見る
- `15/1` のような分母1の分数、正の答えに対する負の選択肢は出さない。
  考えるまでもなく消せてしまう。ただし正負の数の問題では符号ミスこそ本命なので通す
- 4択が作れない問題（誤答が3つそろわない）は**自由入力に落ちる**。点検が件数を出す

## 算数の単元マップ

記録タブにある 25単元 × 5学年のマス目（`renderMathMap`）。
**穴がどこかを見せて、そのまま押して解きに行ける**のが要点。

- マスの色は4段。**凡例の4つとちょうど対応させること。**
  5段にしたときは、暗いテーマで中間の2つが見分けられなかった
- マスは `data-view="math" data-mcat=… data-mlv=…` を持ち、
  `[data-view]` の委譲がまず設定を保存してから画面を切り替える。
  `resetMathToSetup()` が設定を読んで選択欄を合わせ直すので、**この2つは対で直すこと**
- 単元の並びは `MATH_CATEGORIES`。`tools/math-relevel.mjs` の `CATEGORY_ORDER` が決めており、
  **習う順**になっている。単元を足したらそこにも足す（載っていないと emit が落ちる）
- マスごとに `MATH_DATA` を filter しないこと（884問 × 125マス走る）。
  (単元, 学年) で1回だけ振り分けてから使う

## TOEFL Junior 教材の取り込み（完了）

教材3冊のスキャンPDF（`materials/`、git 管理外）から長文読解を取り込み済み。

| 教材 | レベル | ID |
| --- | --- | --- |
| Basic | 2（A2） | r23〜r58 |
| Intermediate | 3（B1） | r59〜r96 |
| Advanced | 4（B2） | r97〜r134 |

作業パイプラインは `tools/` に残してある（import-reading → build-reading-* → emit-reading-*）。
中間ファイル（OCR テキスト・解答・注釈）は `data/raw/reading-import/`（git 管理外）。
新しいデータを足すときは同じ形式で、**解説は日本語**・設問5種類を混ぜる・
正解の位置は 0〜3 に散らす、を守ること。

---

## 文法（Language Usage）の取り込み（完了）

教材3冊の Language Usage セクションから 706 問を取り込み済み。
**教材の解答欄は OCR で復元できなかった**ので、B1・B2 の正解はこちらで文法的に判定した。

```
tools/import-grammar.mjs      A2 の2択（Check-up）。答えは解答欄の語と選択肢を突き合わせて決める
tools/parse-grammar-lfm.mjs   B1・B2 の4択。**選択肢の組だけ**を拾う
tools/grammar-units.mjs       B1・B2 のページ → 単元
tools/grammar-a2/b1/b2.mjs    問題文・正解・解説（人が書いた部分）
tools/emit-grammar.mjs        js/grammar-data.js を書き出す
tools/audit-grammar.mjs       点検
```

作り直しは `node tools/emit-grammar.mjs`、点検は `node tools/audit-grammar.mjs`。
**この順で必ず両方走らせること。**

### なぜ正解を人が書いているか

解答欄が多段組みの表で、スキャンから読むと次のようになる。番号も順序も崩れていて復元できない。

```
1(0)   (c)   IDI   2NC)   (SAD)   16. (c))   33. (Б)
```

そこで**選択肢だけを機械で拾い、正解は1問ずつ判定して書いた**。
判定の根拠はそのまま解説（`j`）として残してあるので、疑わしいものは根拠を読んで直せる。
**根拠を書かずに答えだけ足さないこと。**あとから検算できなくなる。

### 守ること

- **キーはページ内での「何組目か」**（`p31-4`）。教材の設問番号は OCR で飛ぶが、
  選択肢の組の順番は崩れない。設問番号で持つと対応がずれる
- **OCR の傷が選択肢に残っていないか目で見る。**`7ih grade` `SO` のような字面は
  emit の `OCR_FIX` で直すが、直しきれないものは `o` に全文を書いて上書きする
- **単元のページ範囲は、目次ではなく本文中の見出しから取る。**
  A2 で目次から等間隔に割り振ったら**単元が丸ごと1つぶんずれた**
  （助動詞の設問が「受動態」、分詞の設問が「不定詞」になっていた）。
  振り分けたあとは**設問を何問か読んで単元と合っているか確かめる**こと
- **区切りは設問の終わりに合わせる。**Unit Test は次のページにはみ出すので、
  解説の始まりで切ると、はみ出したぶんが隣の単元に混ざる
- **複数単元が混ざるテスト（Chapter Test / Diagnostic / Actual）は「総合」にする。**
  単元を1つ選んで付けると、記録の弱点表示が嘘になる
- **A2 は2択のまま。4択に水増ししない。**もっともらしい誤答を機械で足すと
  「どちらも正しい」問題ができる
- 解説は**全問に日本語で**。点検が「解説が無い」で落とす

### 単語の取り込み

`tools/build-vocab-from-grammar.mjs` が `js/data.js` に追記する。元は次の2つ。

1. **設問文と選択肢**（人が書き写したもの。つづりが確かめてある）
2. **教材の本文にある完全な英文**（解説ページの例文）→ その文を例文として持つ
3. **寸断された地の文**（B1・B2 の長文）→ 語だけを拾い、**例文は付けない**

- 選択肢も使う。**教材が誤答としてわざわざ並べた語**（stationed / illogical / captivating）は
  「この語を知っているか」を試している語なので、覚える価値がいちばん高い
- **本文から拾うときは、語ではなく文を単位にする。**大文字で始まり文末の記号で終わる
  4〜40語の行だけを文とみなし、**その文の語がすべて辞書に当たること**を条件にする。
  1語でも壊れていれば文ごと捨てる。語だけを見ていると `infor` / `mation` のような
  分断に気づけないし、拾った文はそのまま例文として画面に出るので、文の健全性のほうが大事
- 教材は例文を2つ「 / 」でつないで1行に並べることがある。**分けずに拾うと
  無関係な2文がくっついた例文になる**（`I'm packing my backpack. / She is studying linguistics…`）
- **英語について語るための語は落とす**（`METALANGUAGE`）。頻度で並べると
  verb(322) noun(181) participle(121) gerund(82) が上位を独占するが、
  入試で問われるのは英語であって英文法用語ではない。**回数が多い＝重要語ではない**
- **英和辞書は古い語義・専門語義を先頭に置く。**`kite → トビ`、`vaccine → 牛痘種`、
  `racism → 民族主義`、`diet → 議会`。一覧を必ず目で見て `GRAMMAR_MEANINGS` に直す
- **B1・B2 の長文は選択肢の枠に割られていて、②では拾えない。**句点で終わる行にならないため。
  実際 knee / onion / soup / towel / truck のような基本語が落ちていた。
  ③で拾うが、つなぎ直すと段をまたいだ2文がくっついた例文になるので**例文は付けない**。
  OCR の化け（modern → modem）に備えて**2回以上出てくる語だけ**を採る
- **語義にラテン文字が残るものを捨てる規則**（答えが問題文に漏れるのを防ぐ）に、
  `bike`（bicycle の短縮形）`math`（mathematics の略）`mom` のような基本語が
  まとめて引っかかっていた。意味を手で書けば入る
- 見出し語は**その語形そのもの**にする。「いちばん短い候補を選ぶ」ようにしたら
  `number → numb`、`mother → moth`、`brother → broth` になった
- ただし**すでにある語の活用形にすぎないもの**は落とす（work があるのに working は足さない）
- **`build-vocab-from-passages.mjs --emit` は止めてある。**ID を並び順で振るので、
  走らせると手で消した重複語が戻って **id 379 以降が全部ずれる**（実際に走らせて確かめた）。
  語を足すときは追記する道具のほうを使うこと

## PDF の取り込み

`tools/pdf-to-text.py`（Python。ほかの道具は Node だがここだけ Python）。

```bash
pip install pymupdf pillow
python3 tools/pdf-to-text.py 教材.pdf --check     # 判別が先。ここを飛ばさない
```

- **文字入りの PDF に OCR をかけない。** 誤字ゼロのものをわざわざ誤字ありにすることになる
- OCR（tesseract）は **数字を壊す**。「何千キロ」→「何干キロ」を実際に踏んだ。
  取り込んだあとは必ず目で確かめる
- 精度が要るなら `--mode images` で画像にして **Claude に読ませる**。
  `js/reading-data.js` の r23〜r134 はこの方法
- 出力はページごとに分ける。**あとで誤字を見つけたとき元のページに戻れる**ようにするため
- **教材そのものをリポジトリに置かない。**公開リポジトリなので。
  `materials/` と `out/` は `.gitignore` にある

## Git

- 開発は `claude/toeic-vocabulary-app-7v6o1v` ブランチ
- `main` へのマージは許可済み
- **Pull Request は明示的に頼まれたときだけ**作る
